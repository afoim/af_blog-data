# Fix encoding for Chinese markdown files
/posts/*.md
  Content-Type: text/markdown; charset=utf-8

/*.md
  Content-Type: text/markdown; charset=utf-8

# JSON index charset
/posts.json
  Content-Type: application/json; charset=utf-8

# RSS feed
# 用 application/xml 而不是 application/rss+xml：后者浏览器不当可渲染 XML，
# 直接摊成纯文本（无高亮、无折叠）；application/xml 会走 XML 树查看器。
# 两者对 RSS 阅读器都是合法类型，自动发现靠的是 <link> 标签的 type 属性。
/rss.xml
  Content-Type: application/xml; charset=utf-8
